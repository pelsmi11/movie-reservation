spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME_USER_SERVICE:user_db}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:password}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

server:
  port: 60201

movie-service:
  secret:
    access: 1b183f729acb7c4863d8d4d6b1a35e21secretKeyExtraChars
    refresh: 2a273e821bfc6b3749e9e5e7b2b46f22refreshKeyExtraChars